import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ["192.168.1.5","https://my-workout-app-2.netlify.app/", "https://my-workout-app-beta.vercel.app"],
};

export default nextConfig;
